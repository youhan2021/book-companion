# book-companion 配置文件

# 每次摸鱼最小字数（默认100字），持续从队列取直到超过此字数
FISH_MIN_CHARS=100

# 基于回复行数的插入比例（每N行插入1次，最少1次）
# 此配置优先级高于 FISH_INSERT_INTERVAL
FISH_INSERT_RATIO=5

# 基于工具调用间隔（每N次调用插入一条，0表示禁用）
FISH_INSERT_INTERVAL=0
